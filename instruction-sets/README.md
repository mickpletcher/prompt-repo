# Instruction Sets

Store persistent project or tool instruction files in this directory. As the
collection grows, group related instruction sets into tool or project
subdirectories and use descriptive, lowercase kebab-case filenames.
